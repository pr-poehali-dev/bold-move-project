-- Записи журнала с outcome='recovered' были созданы из-за бага сравнения телефонов
-- (номер сравнивался как текст, а не по цифрам) — на самом деле это НЕ спасённые
-- заявки, а дубли уже существующих. Переводим их в 'duplicate' с честной причиной,
-- чтобы журнал не пугал ложной статистикой "вебхук потерял 7 заявок".
UPDATE t_p45929761_bold_move_project.leads_webhook_raw_log
SET outcome = 'duplicate',
    error = 'дубль — оригинал уже был создан вебхуком (ошибка сверки номера, исправлена)'
WHERE outcome = 'recovered';
