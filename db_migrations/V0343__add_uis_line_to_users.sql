-- Линия УИС, к которой относится сотрудник (1 = колл-центр, 2 = наш менеджер).
-- Используется для click-to-call: с какого виртуального номера компании звонить,
-- когда сотрудник инициирует звонок из карточки клиента. NULL = линия не задана,
-- используется номер line1 по умолчанию (обратная совместимость).
ALTER TABLE t_p45929761_bold_move_project.users
  ADD COLUMN IF NOT EXISTS uis_line SMALLINT;

COMMENT ON COLUMN t_p45929761_bold_move_project.users.uis_line IS
  'Линия УИС сотрудника: 1 (колл-центр) или 2 (наш менеджер). NULL = не задана, по умолчанию линия 1.';