-- Ответственный сотрудник у заявки. Раньше такой связи не было вообще:
-- существовало только текстовое поле responsible_phone (просто номер, без связи
-- с учётной записью), поэтому право «видеть только свои заявки» построить было не на чем.
--
-- Заполняется автоматически: кто первым взял заявку в работу (изменил её),
-- тот и становится ответственным. Значение NULL = заявка ничья, «новая»,
-- её видно тем, кому разрешено видеть неназначенные.
ALTER TABLE t_p45929761_bold_move_project.live_chats
  ADD COLUMN IF NOT EXISTS assigned_to INTEGER;

-- Быстрая выборка «мои заявки» и «неназначенные» в списке и канбане
CREATE INDEX IF NOT EXISTS idx_live_chats_assigned_to
  ON t_p45929761_bold_move_project.live_chats (assigned_to);