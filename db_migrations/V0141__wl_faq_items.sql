CREATE TABLE IF NOT EXISTS wl_faq_items (id SERIAL PRIMARY KEY, wl_manager_id INTEGER NOT NULL, title TEXT NOT NULL, content TEXT NOT NULL, used BOOLEAN NOT NULL DEFAULT TRUE, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW())