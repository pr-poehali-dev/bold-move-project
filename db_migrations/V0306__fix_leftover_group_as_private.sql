-- Одна запись успела попасть в базу за 5 минут между обновлением backend и
-- перезапуском worker.py на сервере: старый воркер отправил номер телефона
-- участника ГРУППЫ (у него открытый телефон в приватности), backend (на тот
-- момент ещё без фикса приоритета) создал "личную" карточку клиента вместо
-- привязки к группе по chat_id. Донастраиваем задним числом: помечаем группой.
UPDATE t_p45929761_bold_move_project.touch_clients
SET chat_type = 'group',
    group_title = 'Группа Telegram'
WHERE chat_type = 'private' AND (channel_ids->>'telegram') ~ '^-\d+$';
