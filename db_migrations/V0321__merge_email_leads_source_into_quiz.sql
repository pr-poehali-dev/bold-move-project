-- "Email-заявки" — не отдельный рекламный источник, а резервный канал доставки
-- тех же заявок с квиза (leakad.ru), когда основной вебхук не сработал.
-- Приводим уже созданные карточки к единому источнику "Квиз", чтобы не было
-- дублирования в статистике и на самих карточках.
UPDATE t_p45929761_bold_move_project.live_chats
SET source = 'Квиз'
WHERE source = 'Email-заявки';
